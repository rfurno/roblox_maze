-- Easy 1 baker preset. Seed stays knobbable until freeze (edit seed, then Bake).

export type Preset = {
	mazeId: string,
	seed: number,
	gridWidth: number,
	gridHeight: number,
	layers: number,
	cellSize: number,
	wallHeight: number,
	wallThickness: number,
	floorThickness: number,
	wallTemplate: string,
	algorithm: string,
	bakeSolution: boolean,
	stampPlates: boolean,
	restampPlates: boolean,
	entranceSide: string,
	exitSide: string,
	layerConnector: string?,
}

return {
	EASY_1 = {
		mazeId = "EASY_1",
		seed = 1,
		gridWidth = 6,
		gridHeight = 6,
		layers = 2,
		cellSize = 24,
		wallHeight = 10.7,
		wallThickness = 2,
		floorThickness = 1,
		wallTemplate = "ShippingContainer",
		algorithm = "RecursiveBacktracker",
		bakeSolution = true,
		stampPlates = true,
		restampPlates = false,
		entranceSide = "S",
		exitSide = "N",
	},
	-- One layer: same crate cells as Easy 1, second run in the Easy chain.
	EASY_2 = {
		mazeId = "EASY_2",
		seed = 2,
		gridWidth = 6,
		gridHeight = 6,
		layers = 1,
		cellSize = 24,
		wallHeight = 10.7,
		wallThickness = 2,
		floorThickness = 1,
		wallTemplate = "ShippingContainer",
		algorithm = "RecursiveBacktracker",
		bakeSolution = true,
		stampPlates = true,
		restampPlates = false,
		entranceSide = "S",
		exitSide = "N",
		layerConnector = "stairs",
	},
	MILD_1 = {
		mazeId = "MILD_1",
		seed = 11,
		gridWidth = 12,
		gridHeight = 12,
		layers = 1,
		cellSize = 24,
		wallHeight = 10.7,
		wallThickness = 2,
		floorThickness = 1,
		wallTemplate = "Wall",
		algorithm = "RecursiveBacktracker",
		bakeSolution = true,
		stampPlates = true,
		restampPlates = false,
		entranceSide = "S",
		exitSide = "N",
		layerConnector = "none",
	},
	MILD_2 = {
		mazeId = "MILD_2",
		seed = 12,
		gridWidth = 12,
		gridHeight = 12,
		layers = 2,
		cellSize = 24,
		wallHeight = 10.7,
		wallThickness = 2,
		floorThickness = 1,
		wallTemplate = "Wall",
		algorithm = "RecursiveBacktracker",
		bakeSolution = true,
		stampPlates = true,
		restampPlates = false,
		entranceSide = "S",
		exitSide = "N",
		layerConnector = "elevator",
	},
	HARD_1 = {
		mazeId = "HARD_1",
		seed = 21,
		gridWidth = 12,
		gridHeight = 12,
		layers = 1,
		cellSize = 24,
		wallHeight = 10.7,
		wallThickness = 2,
		floorThickness = 1,
		wallTemplate = "Wall",
		algorithm = "RecursiveBacktracker",
		bakeSolution = true,
		stampPlates = true,
		restampPlates = false,
		entranceSide = "S",
		exitSide = "N",
		layerConnector = "none",
	},
}
