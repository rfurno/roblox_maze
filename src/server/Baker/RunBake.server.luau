-- Disabled Studio command. Enable and play, or require MazeBaker from the command bar:
--   require(game.ServerScriptService.Baker.MazeBaker).Bake("EASY_1")

print("[MazeBaker] RunBake is Disabled — use MazeBaker.Bake from the command bar")
